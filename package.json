{
  "private": "true",
  "name": "dtnb-buildtools",
  "scripts": {
    "build": "node _buildtools/build %npm_config_targets%",
    "test": "node _buildtools/test %npm_config_targets%",
    "codegen": "node _buildtools/codegen %npm_config_targets%"
  }
}
